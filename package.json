{
  "name": "my-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter react-search-box-zn dev",
    "build": "pnpm -r run build",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "vite": "^5.3.0"
  }
}
